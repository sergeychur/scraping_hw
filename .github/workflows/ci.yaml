name: Build and test wikipedia scraper
run-name: Assessing ${{ github.actor }} work
on: [pull_request]
jobs:
  local_part:
    uses: ./.github/workflows/workflow.yaml
    with:
      data-dir: './test_data/local'
